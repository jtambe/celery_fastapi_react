[project]
name = "celery-tasks"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "celery>=5.5.3",
    "redis>=6.4.0",
    "python-dotenv>=1.0.0",
]

[tool.setuptools]
py-modules = ["celery_client", "celery_task"]
